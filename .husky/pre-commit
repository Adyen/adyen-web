# helper if you are running repo inside a container
if which vagrant; then
  vagrant ssh -- -t <<HEREDOC
  cd workspace/adyen-web;
  echo "head -n -1 ~/.bashrc > temp.txt ; mv temp.txt ~/.bashrc && yarn run validate;" >> ~/.bashrc;
  /bin/bash -i
HEREDOC
else
  yarn run validate
fi