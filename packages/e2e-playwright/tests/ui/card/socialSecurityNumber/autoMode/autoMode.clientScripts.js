/**
 * Set koreanAuthenticationRequired & countryCode so KCP fields show at start
 */
window.cardConfig = {
    type: 'scheme',
    brands: ['mc', 'visa', 'amex'],
    configuration: {
        // socialSecurityNumberMode: 'auto' // default value
    }
};
