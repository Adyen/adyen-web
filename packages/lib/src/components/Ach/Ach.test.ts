describe('ACH', () => {
    describe('Default component', () => {
        test('should submit the payment');

        test('should submit the payment with the store consent given');

        test('should not submit the payment if the account number does not match');

        test('should not submit the payment if the account number is invalid');

        test('should not submit the payment if the routing number is invalid');

        test('should hide the holder name if configuration is set to hide it');
    });

    describe('Stored component', () => {
        test('should submit the payment');
    });
});
